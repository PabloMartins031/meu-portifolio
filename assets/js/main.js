

document.addEventListener("DOMContentLoaded", () => {

    console.log("⚔️ Dev Samurai iniciado!");

    // Navegação
    iniciarScrollSuave();
    iniciarMenuAtivo();

    // Animações
    iniciarAnimacoes();

    // Hero
    iniciarTyping();

    // Contato
    iniciarFormulario();

});